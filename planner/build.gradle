
plugins {
    // Apply the application plugin to add support for building a CLI application.
    id 'java-library'
}

dependencies {
    
}

sourceSets {
    main {
        resources {
            srcDirs "src/main/resources", "src/main/prolog"
        }
    }
}