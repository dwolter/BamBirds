package planner;

/**
 * Enum of all Thinkers (s. {@link meta.Strategy}) to differentiate
 * {@link Target}s by their source so they can be handled accordingly.
 */
public enum ThinkerType {
	OLDPLANNER, NEWPLANNER
}
